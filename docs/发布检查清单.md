# 发布检查清单

更新时间：2026-04-30

本文记录 JavaFX 版发布前检查项和交付材料状态。构建产物位于 `JQJava/target/`，受 `.gitignore` 忽略，不应提交到版本库。

## 必做检查

在 `JQJava/` 目录执行：

```powershell
mvn -q test
```

期望结果：全部 JUnit 测试通过。

2026-04-30 验证结果：通过。

```powershell
mvn -q -DskipTests package
```

期望结果：生成主 jar 和运行时依赖目录。

2026-04-30 验证结果：通过。

## Windows app-image

在 `JQJava/` 目录执行：

```powershell
powershell -ExecutionPolicy Bypass -File scripts\package-windows.ps1
```

期望输出：

```text
target\dist\windows\ZangJiuQi-Java
```

可执行文件：

```text
target\dist\windows\ZangJiuQi-Java\ZangJiuQi-Java.exe
```

脚本会先运行测试，再执行 Maven package 和 `jpackage`。脚本包含项目内路径校验，并会清理旧的打包输入目录和旧 app-image。

2026-04-30 验证结果：通过，已生成 app-image。本地构建产物不提交。

## Windows 安装包

可选执行：

```powershell
powershell -ExecutionPolicy Bypass -File scripts\package-windows.ps1 -Installer
```

如果本机缺少安装包依赖，脚本会回退生成 app-image。

## 文档检查

- [当前实现状态](当前实现状态.md)：必须反映当前实际能力。
- [规则覆盖表](规则覆盖表.md)：必须标明已支持、部分支持、未支持规则。
- [路线图](路线图.md)：必须把未实现内容保留在后续阶段。
- [Java 版运行说明](../JQJava/README-运行说明.md)：必须包含运行、测试、打包、拉萨/金鱼可选获胜阵型和弱势方飞子临界保门胜边界。
- [发布材料 v1](发布材料-v1.md)：必须包含版本能力、运行方式、演示流程、AI 对局说明和发布边界。
- [演示脚本 v1](演示脚本-v1.md)：必须包含竞技化、传统阵型补吃和传统获胜三条演示路径。
- [变更记录](变更记录.md)：发布前应补充本次变更摘要。

## 演示材料状态

- [x] JavaFX 发布材料总览。
- [x] 竞技化 `8x8` 演示路径。
- [x] 传统基础 `14x14` 阵型补吃演示路径。
- [x] 拉萨/金鱼可选获胜阵型与弱势方飞子临界保门胜说明。
- [x] AI 对局说明：以自动化测试和回归验证为依据，不提供未实测胜率。
- [x] 截图目录与命名规范：`docs/assets/release-v1/`。
- [ ] JavaFX 主界面截图：`01-main-competitive-8x8.png`。
- [ ] 传统基础 `14x14` 截图：`02-traditional-14x14.png`。
- [ ] 阵型补吃截图：`03-formation-capture.png`。

## 发布边界

- 不宣称已实现剩余特殊阵型、八吉祥整套阵型、飞子阵型或完整传统胜负体系。
- 对外说明传统胜负时，只宣称已支持 `拉萨` / `金鱼` 作为可选获胜阵型及弱势方飞子临界保门胜，不宣称已支持完整固定棋形集合或八吉祥整套制度。
- 不把 `target/` 下 app-image、安装包、压缩包提交到版本库。
- Windows app-image 默认输出到 `JQJava\target\dist\windows\ZangJiuQi-Java`；安装包为可选项。
- native AI 仅承诺 Windows x64 可用；跨平台默认使用纯 Java AI。
